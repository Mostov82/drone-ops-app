# Copy to .env (a ready .env is delivered; both point at the same default)
# SQLite DB lives in app-data/ at the repo root (GB-01 Gate 3 layout), gitignored.
DATABASE_URL="file:../../app-data/drone-ops.db"
